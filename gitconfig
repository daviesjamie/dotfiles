[user]
    name = Jamie Davies
    email = jagd@netcraft.com

[core]
    editor = vim

[push]
    default = simple

[color]
    branch = auto
    diff = auto
    grep = auto
    interactive = auto
    status = auto
    ui = auto

[alias]
    a = add
    c = commit
    co = checkout
    s = status
    uns = reset HEAD

    branches = branch -a
    remotes = remote -v
    tags = tag -l
